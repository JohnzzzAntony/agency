{
  "name": "duoonex-site-cms",
  "version": "1.0.0",
  "private": true,
  "description": "DuooNex static website with GitHub-connected Pages CMS editing",
  "scripts": {
    "start": "npm run build && npm run preview",
    "build": "node scripts/build.js",
    "preview": "node scripts/preview.js",
    "init-content": "node scripts/init-content.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "yaml": "^2.9.1"
  }
}
